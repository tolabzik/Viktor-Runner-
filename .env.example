PORT=8080
# Set 127.0.0.1 when using a reverse proxy on this same host.
BIND_ADDRESS=0.0.0.0
# Use 1 only when the browser accesses the game through HTTPS.
COOKIE_SECURE=0
# Optional canonical browser origin, for example https://runner.example.org
APP_ORIGIN=
